*Социальные сети:*
<% if (socialMedia.length > 0) { %>
    <% socialMedia.forEach(social => { %>
        - *Контакт:* №<%= social.id %>|MP|
        *Название:* <%= social.title %>
        *Ссылка:* <%= social.linkToSM %>
        *Изображение:* <%= social.imgUrl %>
    <% }) %>
<% } else { %>
    Нет доступных социальных медиа.
<% } %>