*Как это работает:*
<% if (works.length > 0) { %>
    <% works.forEach(work => { %>
        - *О работе:* №<%= work.id %>|MP|
        - *Название:* <%= work.title %>
          *Изображение:* <%= work.imgUrl %>
    <% }) %>
<% } else { %>
    Нет доступных работ.
<% } %>